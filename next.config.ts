import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ["www.thecocktaildb.com"],
  },
  outputFileTracingRoot: "/Users/student/MixHaus/mixhaus",
};

module.exports = {
  images: {
    domains: ["www.thecocktaildb.com"],
  },
};

export default nextConfig;
